# Cesium Ion 액세스 토큰 (선택)
# https://cesium.com/ion/tokens 에서 무료 발급 후 위성/terrain 사용
# 없으면 OSM 타일로 기본 지도 표시
REACT_APP_CESIUM_ION_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJqdGkiOiIyMmI3N2NkZS04ZTU4LTQ2ODEtODcwMy01OTk5ZTAxN2FjOGYiLCJpZCI6NDAwMTE2LCJpYXQiOjE3NzI5NTIyMzd9.TSJOl2jZATQ1hajldlbM4c1nlhe7TsmpBLK49M6-B-g

# ACLED: CORS 때문에 브라우저 직접 호출 불가 → 토큰은 backend/.env 의 ACLED_API_TOKEN 에만 두고,
# backend 폴더에서 pnpm install && pnpm start 로 백엔드(5001) 실행 후 frontend 실행.
# REACT_APP_ACLED_API_TOKEN 은 사용하지 않음 (프록시 사용).
# REACT_APP_ACLED_API_TOKEN= (프록시 사용 시 불필요)